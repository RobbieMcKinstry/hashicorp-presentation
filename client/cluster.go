package main

// A Cluster is simply a list of machines
// and other metadata about the state of the simulation.
// It contains all of the simulation-specific data, e.g.
// how many machines are active,
// the names of those machines,
// the current load on the system.
/*
type Cluster struct {
	systemLoad uint64
	machines   [3]*Machine
}
*/
